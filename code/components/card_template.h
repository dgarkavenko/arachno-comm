#pragma once
#include <string>
#include <SFML/Graphics.hpp>

struct CardTemplateComponent{
    std::string texture_path;
    //std::string name;
    sf::Vector2f scale;
};

struct InHand{
    //short order;
};